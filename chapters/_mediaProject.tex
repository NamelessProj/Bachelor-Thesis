\section{Mise en \oe{}uvre}
\label{chap:MediaProject}

Cette partie constitue le cœur du travail. J’y présente la conception et le développement de \textbf{\ns}, un moteur de \gls{vn} \glsit{opensource} entièrement piloté par des fichiers texte au format \gls{json}. L’auteur rédige son histoire dans un fichier \gls{json} conforme au \gls{schema} défini, puis met le projet en ligne soit en compilant l’application \gls{reactjs} en fichiers statiques via \texttt{npm run build}, soit en passant par une plateforme d’intégration continue comme Vercel\footnote{\textbf{Vercel}: plateforme de déploiement cloud spécialisée dans les applications web statiques et les frameworks \gls{js}. Elle automatise la compilation et la mise en ligne à chaque modification du dépôt source. \parencite{noauthor_vercel_nodate}} ou GitHub Pages\footnote{\textbf{GitHub Pages}: service d'hébergement statique intégré à GitHub, permettant de publier automatiquement le contenu d'un dépôt sous forme de site web accessible via une URL publique, sans configuration serveur. \parencite{noauthor_documentation_nodate}}. Le \gls{vn} final reste ainsi accessible depuis n'importe quel navigateur internet moderne, sans installation côté joueur. Le code source du projet est disponible publiquement sur GitHub à l'adresse suivante: \nsrepo.

Le développement s’est inscrit dans une logique itérative: chaque itération correspondait à un ensemble de fonctionnalités implémentées, testées et évaluées avant de passer à l’étape suivante.


\subsection{Évolution du périmètre: de la librairie à l'application} 

Au départ, j’avais prévu de développer \nsits sous forme de \gls{librairie} \gls{reactjs} publiable sur \gls{npm}, intégrable par un développeur tiers via une simple commande d’installation. L’idée était de distribuer le moteur comme un composant réutilisable, indépendamment de toute application hôte.

Afin d'apprendre à mettre en \oe{}uvre cette architecture, deux ressources ont été consultées: une playlist YouTube d'Imran Hussain (son nom d'utilisateur \textit{Imran Codes}) en 2024, intitulée \textit{Building a React Component Library: A Senior Developer's Guide}\parencite{imran_codes_1_2024} (ciblant \gls{reactjs} 16), et un article textuel d'Alex Eagleson publié sur dev.to en novembre 2021, mis à jour en décembre 2022 \parencite{noauthor_how_2021} (ciblant \gls{reactjs} 17 et rollup\footnote{\textbf{Rollup}: outil de bundling \gls{js} spécialisé dans la création de \gls{librairie} distribuables. En mode \gls{librairie} (\textit{\glslink{librairie}{library} mode}), il compile le code source en un ou plusieurs fichiers optimisés, accompagnés des définitions de types \gls{ts}, prêts à être publiés comme \glsit{package} \gls{npm}. \parencite{noauthor_introduction_nodate}} V2). Les deux ressources reposaient sur la même approche: configurer rollup en mode \gls{librairie} via un fichier \texttt{rollup.config.js}, en combinant les \glspl{plugin} \texttt{@rollup/plugin-typescript}, \texttt{@rollup/plugin-commonjs} et \texttt{rollup-plugin-dts}.

\clearpage

Dans les deux cas, la commande de \glsit{build} (\texttt{npm run build}) échouait. Les erreurs rencontrées relevaient de la configuration rollup, dont les exigences ont évolué avec les versions successives de l'outil. L'article d'Eagleson lui-même signale ce problème: une note intégrée au texte indique que certains outils ont été mis à jour et que les instructions telles qu'écrites ne fonctionnent plus. \parencite{noauthor_how_2021} Les commentaires des deux ressources, datant eux aussi d'environ un an, proposaient des correctifs partiels qui n'ont pas permis de résoudre le problème dans le contexte de \gls{reactjs} 19 et des versions actuelles de rollup.

Aucune ressource plus récente, reposant sur une configuration adaptée à \gls{reactjs} 19, n'a pu être identifiée, les tutoriels disponibles utilisaient tous la même approche de configuration, devenue incompatible avec les versions actuelles de l'outillage. Il n'est pas exclu que certaines erreurs rencontrées aient résulté d'une mauvaise interprétation des étapes de configuration de ma part, cette possibilité ne peut être écartée.

Face à ces obstacles et aux contraintes de temps du projet, la décision a été prise de réorienter l'architecture vers une application \gls{reactjs} autonome. Cette approche, plus directement maîtrisable, permet d'atteindre l'objectif principal du projet: rendre la création de \gls{vn} accessible via le web, sans nécessiter la distribution d'un \glsit{package} \gls{npm}.

\clearpage


\subsubsection{Stack technique et outils}

Le développement de \nsits repose sur les technologies et outils énumérés dans le tableau \ref{tab:stack}.

\begin{table}[H]
    \centering
    \caption{Stack technique et outil utilisés}
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{p{4cm} p{9cm}}
        \hline
        \textbf{Outil / Technologie} & \textbf{Rôle dans le projet} \\
        \hline
        \gls{reactjs} & \Gls{librairie} \gls{js} utilisée pour le rendu du moteur dans le navigateur. \\
        \addlinespace
        \gls{vite} & Outil de \glsit{build} et serveur de développement, permettant un rechargement à chaud rapide lors du développement. \\
        \addlinespace
        \gls{ts} & Surcouche typée de \gls{js}, utilisée pour renforcer la robustesse du code et faciliter la détection d'erreurs avant l'exécution. \\
        \addlinespace
        \gls{json} & Format de description du contenu narratif, servant de source unique de vérité pour le moteur \\
        \addlinespace
        GitHub & Plateforme d'hébergement du code source et de la gestion des versions via \gls{git}. \\
        \hline
    \end{tabular}
    \label{tab:stack}
\end{table}

\clearpage


\subsubsubsection{Mécanisme d'optimisation React.js} \label{sec:optim_react}

\gls{reactjs} propose plusieurs mécanismes permettant de réduire le nombre de \glspl{rendu} inutiles et d'améliorer les performances d'une application. Trois d'entre eux ont été utilisés dans le cadre de ce projet \parencite{noauthor_reference_nodate}:

\texttt{React.memo} est une fonction qui \glslink{override}{court-circuite} le \gls{rendu} d'un composant si ses \texttt{props}\footnote{\textbf{Props} (\textit{properties}): en \gls{reactjs}, objet en lecture seule transmis à un composant par son composant parent pour configurer son affichage ou son comportement. \parencite{noauthor_reference_nodate}} n'ont pas changé depuis le \gls{rendu} précédent. Sans ce mécanisme, tout changement d'état d'un composant parent déclenche le recalcul de l'ensemble de ses composants enfants, qu'ils soient concernés ou non par le changement.

\texttt{useMemo} est un \glsit{hook} \gls{reactjs} permettant de mettre en cache le résultat d'un calcul coûteux entre deux \glspl{rendu}. Le calcul n'est relancé que si l'une de ses dépendances déclarées a changé, évitant ainsi des recalculs inutiles à chaque \gls{rendu}.

\texttt{useRef} est un \glsit{hook} permettant de persister une valeur entre les \glspl{rendu} sans déclencher de \glslink{rendu}{re-rendu} lors de sa modification. Il est utilisé pour stocker des références à des éléments du \acrshort{dom} ou des valeurs internes dont le changement ne doit pas provoquer de mise à jour de l'interface.


\subsubsection{Méthode de développement}

Le développement a suivi un processus itératif basé sur le cycle \glsit{pdca} (\textit{Plan, Do, Check, Act}), tel que défini dans la méthodologie présentée au chapitre \ref{sec:PDCA}. Chaque itération correspond à un cycle complet: planification des fonctionnalités à implémenter, développement, évaluation par tests fonctionnels, puis décision sur les ajustements à apporter à l'itération suivante.

Ce modèle a été retenu pour sa capacité à intégrer les retours d'évaluation de manière continue, sans nécessiter de redéfinir l'ensemble du périmètre à chaque étape.

\clearpage


\subsection{Contraintes et limites}

\subsubsection{Contrainte de déploiement}

Contrairement aux moteurs de jeux traditionnels qui compilent le projet en un exécutable natif, \nsits repose sur des technologies web. Pour créer un \gls{vn} avec ce moteur, l'auteur doit télécharger le projet, puis rédiger son histoire dans un fichier \gls{json} selon le \gls{schema} défini. Le résultat peut ensuite être mis en ligne de deux manières:

\begin{itemize}
    \item En compilant l'application \gls{reactjs} en fichiers statiques via la commande \texttt{npm run build}, puis en hébergeant ces fichiers sur n'importe quel serveur web ou hébergeur statique.
    \item En connectant le dépôt à une plateforme d'intégration continue telle que Vercel ou GitHub Pages, qui se charge automatiquement de la compilation et du déploiement à chaque modification du projet.
\end{itemize}

Dans les deux cas, le \gls{vn} final est accessible depuis n'importe quel navigateur internet moderne, sans installation requise côté joueur, voir la figure \ref{fig:deploiement-flux}.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance = 0.3cm and 0.3cm,
        every node/.style = {font=\scriptsize, align=center},
        box/.style = {
            rectangle, rounded corners=3pt,
            draw=clrBorder, line width=0.7pt,
            minimum width=2.1cm, minimum height=0.7cm,
            inner sep=3pt
        },
        auteur/.style = {box, fill=clrAuteur},
        build/.style  = {box, fill=clrBuild},
        cloud/.style  = {box, fill=clrCloud},
        player/.style = {box, fill=clrPlayer},
        alt/.style    = {box, fill=clrAlt},
        arrow/.style  = {-{Latex[length=4pt,width=3pt]},
                         line width=0.7pt, color=clrArrow},
        lbl/.style    = {font=\tiny\itshape, color=clrNote},
    ]

%% ── Rangée 1 : Préparation (Auteur) ─────────────────────────────────────────
\node[auteur] at (0,0) (dl)
    {Récupération du projet\\
     \tiny{\texttt{git clone} / ZIP}};

\node[auteur, right=of dl]      (content)
    {Rédaction JSON + assets\\
     \tiny{histoire, personnages, médias}};

\node[auteur, right=of content] (config)
    {Config. \texttt{App.tsx}\\
     \tiny{\texttt{scriptFile=...}}};

\node[auteur, right=of config]  (push)
    {Commit \& push\\
     \tiny{sur GitHub}};

\draw[arrow] (dl)      -- (content);
\draw[arrow] (content) -- (config);
\draw[arrow] (config)  -- (push);

%% ── Rangée 2, gauche : Voie CI/CD ───────────────────────────────────────────
\node[cloud] at (-0.8,-1.8) (cicd)
    {Détection du push\\
     \tiny{Vercel / GitHub Pages}};

\node[build, right=of cicd]     (buildci)
    {Build\\
     \tiny{\texttt{tsc \&\& vite} $\to$ \texttt{dist/}}};

\node[cloud, right=of buildci]  (deployci)
    {Déploiement auto\\
     \tiny{URL publique}};

\draw[arrow] (cicd)    -- (buildci);
\draw[arrow] (buildci) -- (deployci);

%% ── Rangée 2, droite : Voie manuelle ─────────────────────────────────────────
\node[build] at (7.2,-1.8) (buildman)
    {Build local\\
     \tiny{\texttt{npm run build} $\to$ \texttt{dist/}}};

\node[alt, right=of buildman]   (upload)
    {Upload manuel\\
     \tiny{hébergeur statique}};

\node[alt, right=of upload]     (deployman)
    {Déploiement\\
     \tiny{URL publique}};

\draw[arrow] (buildman) -- (upload);
\draw[arrow] (upload)   -- (deployman);

%% ── Flèches push → voies ────────────────────────────────────────────────────
\draw[arrow] (push.south) -- ++(0, -0.4cm) -| (buildman.north);
\draw[arrow] (push.south) -- ++(0,-0.4cm) -| (cicd.north);

%% ── Rangée 3 : Exécution (Joueur) ───────────────────────────────────────────
\node[player] at (6.4,-3.6) (browser)
    {Navigateur\\
     \tiny{chargement de l'URL}};

\node[player, right=of browser] (zodwk)
    {Validation Zod\\
     \tiny{+ Web Worker}};

\node[player, right=of zodwk]   (vn)
    {Rendu du VN\\
     \tiny{composant \texttt{VNPlayer}}};

\draw[arrow] (deployci)  |- (browser.west);
\draw[arrow] (deployman) |- (4.4cm, -2.5cm) |- (browser.west);
\draw[arrow] (browser) -- (zodwk);
\draw[arrow] (zodwk)   -- (vn);

%% ── Boîtes de regroupement ───────────────────────────────────────────────────
\begin{scope}[on background layer]
  \node[draw=clrBorder, dotted, rounded corners=5pt, fill=clrAuteur!20,
        fit=(dl)(content)(config)(push), inner sep=6pt,
        label={[font=\tiny\bfseries, text=clrBorder]above:Préparation (Auteur)}
       ] {};

  \node[draw=clrBorder, dotted, rounded corners=5pt, fill=clrCloud!20,
        fit=(cicd)(buildci)(deployci), inner sep=6pt,
        label={[font=\tiny\bfseries, text=clrBorder, yshift=-2pt]above:Voie CI/CD}
       ] {};

  \node[draw=clrBorder, dotted, rounded corners=5pt, fill=clrAlt!20,
        fit=(buildman)(upload)(deployman), inner sep=6pt,
        label={[font=\tiny\bfseries, text=clrBorder, yshift=-2pt]above:Voie manuelle}
       ] {};

  \node[draw=clrBorder, dotted, rounded corners=5pt, fill=clrPlayer!20,
        fit=(browser)(zodwk)(vn), inner sep=6pt,
        label={[font=\tiny\bfseries, text=clrBorder, yshift=-2pt]above:Exécution (Joueur)}
       ] {};
\end{scope}

    \end{tikzpicture}
    \caption{Flux de déploiement de \nsit, de la configuration locale à l'exécution dans le navigateur (Auteur 2026)}
    \label{fig:deploiement-flux}
\end{figure}

\clearpage

\subsubsection{Contrainte de format}

Une contrainte fondamentale de ce projet est de rendre le contenu narratif lisible et interprétable à la fois par un humain et par la machine. Le format \gls{json} est retenu pour répondre à cette exigence: son modèle clé/valeur est suffisamment structuré pour être analysé de manière déterministe par le \gls{parseur}, tout en restant accessible à un auteur non-développeur disposé à respecter un \gls{schema} documenté.

Cependant, cette contrainte implique une limite inhérente: l'absence d'interprétation sémantique du langage naturel signifie que l'auteur doit respecter strictement la \gls{syntaxe} définie. Une erreur de structure dans le fichier \gls{json} peut bloquer l'exécution du moteur. La gestion de ces erreurs et le retour d'informations claires à l'auteur constituent donc un objectif de robustesse à adresser au fil des itérations.


\subsubsection{Contrainte de périmètre}

Pour des raisons de temps, le périmètre fonctionnel du moteur est limité aux mécaniques fondamentales des \gls{vn}, telles que définies dans le tableau \ref{tab:objectifs_moscow}. D'autres fonctionnalités, telles que la gestion de la musique de fond, des images de personnages ou d'un système de sauvegarde, sont identifiées comme souhaitables, mais ne font pas partie des objectifs primaires de ce travail.


\subsection{Étapes de développement}

Le développement de \nsits s'est déroulé en trois itérations successives, chacune suivant  le cycle \glsit{pdca} défini au chapitre \ref{sec:PDCA}. Les objectifs de chaque itération ont été définis en amont selon la priorisation \glsit{moscow} établie au tableau \ref{tab:objectifs_moscow}, et ajustés en fonction des résultats observés à l'issue de chaque cycle.


\subsubsection{Première itération: les bases d'un VN}


\subsubsubsection{Plan}

L'objectif de cette première itération était d'implémenter les mécaniques fondamentales du moteur: l'affichage progressif du texte (effet machine à écrire), la navigation entre scènes et la gestion des choix de dialogue. Ces trois fonctionnalités correspondent aux exigences \textit{Must} du tableau \ref{tab:objectifs_moscow}. Il s'agissait également de définir la structure du \gls{schema} \gls{json} servant de source de contenu, c'est-à-dire les types de n\oe{}uds que le moteur devrait être capable d'interpréter: dialogue, choix, scènes et personnages.


\subsubsubsection{Do}

J'ai commencé par créer le projet. Pour ce faire, il me fallait choisir un langage, un \glsit{framework} et une structure. Pour le langage, j'ai opté pour \gls{ts}, afin de bénéficier d'un typage statique offrant une sécurité dès la phase de développement, avant même l'exécution. Concernant le \glsit{framework}, j'ai hésité avec Next.js\footnote{\textbf{Next.js}: \glsit{framework} \gls{reactjs} orienté rendu serveur, développé par Vercel \parencite{noauthor_nextjs_nodate}}, mais ce dernier aurait été surdimensionné pour ce projet: \nsits ne nécessite aucun traitement côté serveur (\textit{\glsit{backend}}), tout étant calculé dans le \glsit{frontend}. J'ai donc opté pour \gls{reactjs}, une \gls{librairie} \gls{js} que je connais déjà et qui est parfaitement adaptée à la création de ce projet basé sur des composants, ce qui permet de séparer chaque partie du code.

Avant même de commencer à créer un seul composant, j'ai commencé à réfléchir à la structure que je souhaitais utiliser pour le fichier \gls{json} qui contiendra l'histoire. Je voulais quelque chose de personnalisable, afin de laisser un maximum de liberté aux personnes souhaitant utiliser ce projet. Pour atteindre cet objectif, il m'a fallu séparer plusieurs éléments, illustrés dans le tableau \ref{tab:obj_json_schema}.

\begin{table}[H]
    \centering
    \caption{Séparation des éléments principaux du fichier JSON}
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{p{4cm} p{9cm}}
        \hline
        \textbf{Objet} & \textbf{Description} \\
        \hline
        Les paramètres & Définitions des valeurs par défaut globales de l'application: personnalisation de l'écran titre, de la page de crédits, et des comportements d'affichage. Ces valeurs peuvent être \glslink{override}{surchargées} localement au niveau d'un dialogue. \\
        \addlinespace
        Les personnages & La déclaration des personnages afin d'avoir à un seul endroit tous les personnages et leurs informations. \\
        \addlinespace
        L'histoire & Tous les éléments qui serviront à raconter l'histoire. \\
        \hline
    \end{tabular}
    \label{tab:obj_json_schema}
\end{table}

Pourquoi ces choix? Pourquoi ne pas mettre les personnages dans l'histoire? Plusieurs réponses à ces questions. Pour commencer, cela donne une structure simple et lisible, tout est séparé selon des objectifs bien précis: ce qui aide l'auteur (les paramètres), ce qui sera utilisé plusieurs fois (les personnages) et le récit. Les personnages, à l'image des variables, sont susceptibles d'apparaître dans de nombreuses scènes. Les centraliser évite ainsi toute répétition dans le fichier. 

\clearpage

L'extrait de code \ref{lst:json_example} montre cette séparation dans un court exemple.

\begin{listing}[H]
\begin{minted}{json}
{
    "settings": {
        "startingScene": "start",
        "textSpeed": 45
    },
    "characters": {
        "Alice": {
            "name": "Alice",
            "fullName": "Alice Wonderland",
            "color": "#e07ae0"
        }
    },
    "story": {
        "start": {
            "background": "bg.png",
            "dialogues": [
                {
                    "name": "Alice",
                    "text": "Hello, world."
                },
                {
                    "name": "Alice",
                    "text": "Where shall we go?",
                    "options": [
                        { "text": "The library", "next": "library" },
                        { "text": "The park", "next": "park" }
                    ]
                }
            ]
        }
    }
}
\end{minted}
\caption{Exemple de structure de fichier JSON (Auteur 2026)}
\label{lst:json_example}
\end{listing}

\clearpage

Au fur et à mesure des itérations, le \gls{schema} s'enrichira afin de proposer davantage d'options. Cette approche rend l'ensemble du rendu visuel du \gls{vn} personnalisable directement depuis le fichier \gls{json}, sans qu'il soit nécessaire de modifier le code du moteur: image de fond de chaque scène (clé \texttt{background}), couleur associée à chaque personnage, ainsi que l'apparence de l'écran titre et de la page de crédits, définis au niveau des paramètres (voir tableau \ref{tab:obj_json_schema}). La gestion des polices d'écriture via des variables \acrshort{css}, ajoutée lors de la troisième itération, complète cette personnalisation visuelle.

Une fois la structure définie, il me fallait commencer le développement. Pour ce faire, il fallait choisir par quoi commencer, et j'ai décidé de commencer par les composants visuels de base. C'est-à-dire par l'écran de titre, l'écran des crédits et un composant d'image de fond.

Comme la page de titre, les crédits et les scènes auront des images de fond. Pour rendre cet usage plus simple, j'ai pris la décision d'en faire un composant à part entière pour m'éviter des répétitions.

La raison pour laquelle j'ai commencé par les écrans de titre et de crédits au lieu du c\oe{}ur du projet, le \gls{vn}, est que je voulais d'abord me consacrer aux éléments simples qui n'allaient pas avoir beaucoup d'évolution. Une fois ces écrans réalisés, je pouvais me consacrer entièrement au \gls{vn} tout en sachant que les autres écrans étaient déjà faits.

Une fois ces éléments réalisés, j'ai commencé à travailler sur le composant que je considère comme le plus important: l'affichage du texte, avec l'effet machine à écrire. Pour rendre ce composant plus puissant, mon intention était de faire en sorte qu'il puisse \glslink{parseur}{parser} des balises \acrshort{html} et de pouvoir mettre en place une \gls{syntaxe} de pauses afin de donner du tonus aux textes. J'ai donc créé un système de \gls{tokenisation} du texte. L'idée est simple: avant l'affichage, le texte brut est découpé en unités, les \textit{tokens}, qui peuvent être soit du texte ordinaire, soit des balises \acrshort{html}, ou encore des temps de pause, comme l'illustre la figure \ref{fig:tokenisation-flux}. Cette séparation permet à l'effet machine à écrire de n'animer que les caractères visibles, sans traiter les balises comme du texte à afficher. Sans cette étape intermédiaire, afficher \texttt{<b>mot</b>} caractère par caractère aurait risqué de produire des balises partielles visibles à l'écran, ou un \acrshort{html} mal formé.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
    node distance = 0.55cm and 2.2cm,
    every node/.style = {font=\small, align=center},
    box/.style = {
        rectangle, rounded corners=3pt,
        draw=clrBorder, line width=0.8pt,
        minimum width=4.2cm, minimum height=0.9cm,
        inner sep=5pt
    },
    input/.style   = {box, fill=clrInput},
    process/.style = {box, fill=clrProcess},
    token/.style   = {rectangle, rounded corners=3pt,
                      draw=clrBorder, line width=0.7pt, fill=clrToken,
                      minimum width=3.0cm, minimum height=0.8cm,
                      inner sep=4pt, font=\small, align=center},
    output/.style  = {box, fill=clrOutput},
    arrow/.style   = {-{Latex[length=5pt,width=4pt]},
                      line width=0.8pt, color=clrArrow},
    darrow/.style  = {-{Latex[length=4pt,width=3.5pt]},
                      line width=0.7pt, color=clrArrow, dashed},
    lbl/.style     = {font=\scriptsize\itshape, color=clrNote}
    ]

%% ── Colonne principale (gauche) ─────────────────────────────────────────────
 
\node[input] (raw)
    {Texte brut \texttt{text}\\
     \footnotesize{(champ du fichier \gls{json})}};
 
\node[process, below=of raw] (escape)
    {\texttt{escapeHtml()}\\
     \footnotesize{Protège \texttt{\&}, \texttt{<}, \texttt{>} du HTML}};
 
\node[process, below=of escape] (vars)
    {\texttt{getTextWithCharacters()}\\
     \footnotesize{Résout \texttt{\{\{Alice\}\}} en balise colorée}};
 
\node[process, below=of vars] (markup)
    {\texttt{parseMarkup()}\\
     \footnotesize{Convertit \texttt{[b]}, \texttt{[color]}, \texttt{[link]} en HTML}};
 
\node[process, below=of markup] (tokenize)
    {\texttt{tokenizeHtmlWithPauses()}\\
     \footnotesize{Sépare balises, texte et pauses}};
 
\node[process, below=1.5cm of tokenize] (precompute)
    {\texttt{precomputeSteps()}\\
     \footnotesize{Précalcul O(n) des \glslink{snaphtml}{snapshots HTML} et délais}};
 
\node[process, below=of precompute] (react)
    {\texttt{htmlToReactNode()}\\
     \footnotesize{\glslink{snaphtml}{Snapshots HTML} convertis en noeuds React}};
 
\node[output, below=of react] (render)
    {Composant \texttt{Typewriter}\\
     \footnotesize{Affiche un \glslink{snaphtml}{snapshot} par tick via \texttt{setTimeout}}};
 
%% ── Flèches verticales ──────────────────────────────────────────────────────
\draw[arrow] (raw)        -- (escape);
\draw[arrow] (escape)     -- (vars);
\draw[arrow] (vars)       -- (markup);
\draw[arrow] (markup)     -- (tokenize);
\draw[arrow] (tokenize)   -- node[lbl, right=2pt] {vers tokens} (precompute);
\draw[arrow] (precompute) -- (react);
\draw[arrow] (react)      -- (render);
 
%% ── Annotations latérales (gauche) ─────────────────────────────────────────
\node[lbl, left=4pt of escape]    {sécurité};
\node[lbl, left=4pt of vars]      {interpolation};
\node[lbl, left=4pt of markup]    {mise en forme};
\node[lbl, left=4pt of tokenize]  {découpage};
\node[lbl, left=4pt of precompute]{optimisation};
\node[lbl, left=4pt of react]     {\gls{memoisation}};
 
%% ── Tableau de tokens (droite) ──────────────────────────────────────────────
\node[token, right=of tokenize, yshift= 1.6cm] (tok_open)
    {\texttt{openTag}\\
     \footnotesize{ex. \texttt{<b>}}};
 
\node[token, below=0.35cm of tok_open] (tok_close)
    {\texttt{closeTag}\\
     \footnotesize{ex. \texttt{</b>}}};
 
\node[token, below=0.35cm of tok_close] (tok_self)
    {\texttt{selfClosingTag}\\
     \footnotesize{ex. \texttt{<br/>}}};
 
\node[token, below=0.35cm of tok_self] (tok_text)
    {\texttt{text}\\
     \footnotesize{caractères visibles}};
 
\node[token, below=0.35cm of tok_text] (tok_pause)
    {\texttt{pause}\\
     \footnotesize{\texttt{\textbackslash .}=1000 ms, \texttt{\textbackslash ,}=500 ms}};
 
%% Boîte englobante tokens
\begin{scope}[on background layer]
  \node[draw=clrBorder, dashed, rounded corners=4pt,
        fill=clrToken!25,
        fit=(tok_open)(tok_close)(tok_self)(tok_text)(tok_pause),
        inner sep=8pt,
        label={[font=\footnotesize\bfseries, text=clrBorder]above:\texttt{Token[]} — types possibles}
       ] (tokbox) {};
\end{scope}
 
%% ── Flèches vers/depuis le tableau de tokens ────────────────────────────────
\draw[darrow] (tokenize.east)  -- ([xshift=-2pt, yshift=1.4cm]tokbox.west)
              node[midway, lbl, above=1pt] {produit};
 
\draw[darrow] ([yshift=-1.6cm]tokbox.west)  |- (precompute.east);
    
    \end{tikzpicture}
    \caption{Flux de tokenisation du composant \texttt{Typewriter} (Auteur 2026)}
    \label{fig:tokenisation-flux}
\end{figure}

Une fois tous ces éléments fonctionnels, le temps était venu pour moi de mettre en place la logique première des \gls{vn}, une navigation entre dialogues et entre scènes. J'ai créé un composant de dialogue, contenant le texte et, par la suite, également le nom de la personne qui parle. J'ai aussi créé le composant de scène, dans lequel le texte et l'image de fond sont réunis.

Pour l'interactivité, lors d'un clic ou lorsque le joueur appuie sur la touche \textit{Entrée}, soit le dialogue change pour le prochain de la scène courante, soit, si nous sommes au dernier dialogue de la scène, nous sommes amenés à la scène définie par la clé \texttt{next} du dernier dialogue, ou à la page des crédits si cette clé est absente ou si la valeur de la clé est \texttt{"\_\_end\_\_"}.

\clearpage

Un autre élément ajouté fut la possibilité de faire des choix. J'ai veillé à ce que des boutons puissent apparaître à l'écran, permettant au joueur de choisir quelle voie suivre. Parmi les possibilités, le joueur peut se retrouver dans une autre scène, à un autre dialogue de la scène courante ou à un dialogue précis d'une autre scène. L'auteur peut également choisir d'amener le joueur sur la page des crédits en mettant \texttt{"\_\_end\_\_"} comme valeur dans la clé \texttt{next}.

La figure \ref{fig:NamelessStoryV1} présente l'état du moteur à l'issue de cette première itération.\footnote{Par souci de lisibilité à l'impression, l'ensemble des captures d'écran du moteur présentées dans ce chapitre a été réalisé avec un zoom du navigateur fixé à 175\%. Le rendu réel, au zoom par défaut du navigateur (100\%), affiche donc des éléments proportionnellement plus petits que ceux visibles sur ces figures.\label{fn:zoom175}}

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.47\textwidth}
        \includegraphics[width=\textwidth]{img/NamelessStory_V1_home.png}
        \caption{Écran titre}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.47\textwidth}
        \includegraphics[width=\textwidth]{img/NamelessStory_V1_text.png}
        \caption{Affichage d'un dialogue}
    \end{subfigure}
    \caption{\nsits à l'issue de la première itération (Auteur 2026)}
    \label{fig:NamelessStoryV1}
\end{figure}

\clearpage


\subsubsubsection{Check}

À l'issue de cette itération, les mécaniques fondamentales étaient fonctionnelles et vérifiables dans le navigateur à partir d'un fichier \gls{json} de test. Les cas valides ont été testés manuellement: progression du dialogue au clic, navigation vers une autre scène, affichage des choix et sélection d'une option. Des scénarios d'erreur ont également été construits intentionnellement (\glsit{negativetesting}), notamment en fournissant des fichiers \gls{json} structurellement incomplets, afin de vérifier le comportement du moteur face à des données inattendues.

Ces tests ont révélé que le moteur adoptait un comportement indéfini face à des données manquantes ou erronées, sans retourner d'informations exploitables à l'auteur: pas de message d'erreur, aucun moyen de savoir quoi corriger.

M’étant principalement focalisé sur les fonctionnalités, le style laissait encore à désirer. L'essentiel pour moi à ce stade du projet était d'avoir des éléments affichés à l'écran, pas nécessairement qu'ils soient jolis.


\subsubsubsection{Act}

Ayant une base fonctionnelle, deux options s'offraient à moi pour la suite: traiter le problème des messages d'erreur ou optimiser les performances du moteur. Ne possédant pas encore les connaissances nécessaires en matière de robustesse et de validation, j'ai choisi de m'engager dans un travail d'optimisation afin de garantir que le moteur évite les recalculs inutiles et exploite efficacement les ressources du navigateur.

\clearpage


\subsubsection{Deuxième itération: l'optimisation}

\subsubsubsection{Plan}

L'objectif de cette itération était d'améliorer les performances et la maintenabilité du code produit lors de la première itération. Dans \gls{reactjs}, chaque mise à jour de l'état d'un composant parent déclenche un nouveau \gls{rendu} de l'ensemble de l'arbre de composants enfants. Dans un moteur de \gls{vn}, où l'état global évolue à chaque ligne de dialogue, ce comportement peut provoquer des recalculs inutiles sur des composants dont les données n'ont pas changé. un second axe concernait la gestion de la sauvegarde: l'opération de sérialisation de l'état courant via \texttt{JSON.stringify()} s'exécutait sur le fil principal du navigateur. Cette opération ne pose pas de problème tant que l'objet à sérialiser reste de taille modeste. Cependant, à mesure que l'état s'enrichit (variables de jeu, historique de dialogue, données de progression), cette opération peut devenir coûteuse et bloquer l'interface le temps de son exécution. Afin d'anticiper cette limitation, la décision a été prise de déléguer cette opération à un \gls{webworker}.


\subsubsubsection{Do}

L'optimisation du \gls{rendu} a porté sur l'ensemble des composants du moteur. Chaque composant a été mémorisé via \texttt{React.memo} (voir section \ref{sec:optim_react}), ce qui permet à \gls{reactjs} de court-circuiter le \gls{rendu} d'un composant si ses \texttt{props} n'ont pas changé depuis le \gls{rendu} précédent. Pour les calculs coûteux effectués à l'intérieur des composants, le \glsit{hook} \texttt{useMemo} a été utilisé afin de mettre les résultats en cache entre les \glspl{rendu}. Des références (\texttt{useRef}) ont également remplacé certains états React où une valeur devait persister sans déclencher de \glslink{rendu}{re-rendu}. Le composant \texttt{Typewriter} a notamment fait l'objet d'une refactorisation significative: les \glslink{snaphtml}{snapshots HTML} et les délais entre caractères sont désormais précalculés en une seule passe avant le début de l'animation, évitant tout recalcul durant la frappe.

L'extrait de code \ref{lst:typewriter} illustre la stratégie d'optimisation du composant \texttt{Typewriter}. Avant le début de l'animation, \texttt{precomputeSteps} calcule en une seule passe l'ensemble des \glslink{snaphtml}{snapshots HTML} et des délais associés à chaque caractère. Ces snapshots sont ensuite convertis en n\oe{}uds React via un second \texttt{useMemo}, dont les dépendances sont strictement séparées du premier. Durant l'animation, le \texttt{useEffect} de tick se limite à lire \texttt{nodeSnapshots[currentStep]} et de planifier le prochain appel via \texttt{setTimeout} avec le délai précompute \texttt{delays[currentStep]}: aucun calcul n'est effectué à chaque frappe. 

\begin{listing}[H]
\begin{minted}{ts}
// Pré-calcul unique : 0(n) avant l'animation
const precomputed = useMemo(
    () => TypewriterUtils.precomputeSteps(tokens, speed),
    [tokens, speed]
);

// Conversion des snapshots HTML en nœuds react (une seule fois)
const nodeSnapshots: ReactNode[] = useMemo(
    () => precomputed.htmlSnapshots.map(htmlToReactNode),
    [precomputed]
);

// Avancement : lecture directe à l'index | 0(1) par tick
useEffect(() => {
    if (currentStep >= totalSteps) {
        if (!completedRef.current) {
            completedRef.current = true;
            onComplete?.();
        }
        return;
    }
    const delay: number = precomputed.delays[currentStep] ?? speed;
    const timeout: number = setTimeout(
        () => setCurrentStep((prev) => prev + 1),
        delay
    );
    return () => clearTimeout(timeout);
}, [currentStep, totalSteps, precomputed, speed, onComplete]);
\end{minted}
\caption{Précomputation des instantanés HTML et des délais dans le composant \textit{Typewriter} (Auteur 2026)}
\label{lst:typewriter}
\end{listing}

En parallèle, le traitement de la mise en forme du texte a été revu pour des raisons de sécurité et de robustesse. La propriété React \texttt{dangerouslySetInnerHTML} qui permettait d'injecter directement du \acrshort{html} dans le \acrshort{dom}, a été remplacée par un couple de fonctions dédiées: \texttt{escapeHtml}, qui neutralise les caractères spéciaux avant tout traitement, et \texttt{htmlToReactNode}, qui convertit la chaîne résultante en n\oe{}uds React natifs. Cette approche supprime le risque d'injection de contenu malveillant tout en conservant la flexibilité du balisage personnalisé. Pour garder cette personnalisation des balises dans le texte, un \gls{parseur} inspiré de \gls{markdown} a été mis en place.

Pour la sauvegarde, un \gls{webworker} dédié (\texttt{saveWorker}) a été mis en place. Le fil principal transmet l'état courant au \gls{webworker} par échange de messages, celui-ci exécute \texttt{JSON.stringify()} dans son propre fil d'exécution, puis retourne la chaîne sérialisée au fil principal, comme illustré à la figure \ref{fig:webworker-architecture}. Cette communication est entièrement asynchrone: l'interface utilisateur reste réactive pendant toute la durée de l'opération, quelle que soit la taille de l'objet à sérialiser.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
    every node/.style = {font=\small, align=center},
    % Boîtes internes
    box/.style = {
        rectangle, rounded corners=3pt,
        draw=clrBorder, line width=0.8pt,
        minimum width=3.2cm, minimum height=0.85cm,
        inner sep=5pt
    },
    main/.style  = {box, fill=clrInput},
    work/.style  = {box, fill=clrToken},
    proc/.style  = {box, fill=clrProcess},
    % Flèches
    arrow/.style = {-{Latex[length=5pt,width=4pt]},
                    line width=0.8pt, color=clrArrow},
    asyncarrow/.style = {-{Latex[length=6pt,width=5pt]},
                    line width=1.2pt, color=clrBorder},
    lbl/.style   = {font=\scriptsize\itshape, color=clrNote},
    lblbf/.style = {font=\scriptsize\bfseries, color=clrBorder},
    ]

%% ═══════════════════════════════════════════════════════
%% COLONNE GAUCHE — Fil principal
%% ═══════════════════════════════════════════════════════
 
% Nœuds internes fil principal (x = -3.8cm)
\node[main] at (-3.8cm,  2.6cm) (ui)
    {Rendu de l'interface\\
     \footnotesize{utilisateur (60 FPS)}};
 
\node[main] at (-3.8cm,  0.5cm) (input)
    {Interactions utilisateur\\
     \footnotesize{(clics, saisie)}};
 
\node[main] at (-3.8cm, -1.6cm) (dom)
    {Mise à jour du DOM};
 
% Flèches internes fil principal (double sens)
\draw[arrow, -{Latex[length=4pt,width=3.5pt]}]
    ([xshift= 6pt]ui.south)    -- ([xshift= 6pt]input.north);
\draw[arrow, -{Latex[length=4pt,width=3.5pt]}]
    ([xshift=-6pt]input.north) -- ([xshift=-6pt]ui.south);
 
\draw[arrow, -{Latex[length=4pt,width=3.5pt]}]
    ([xshift= 6pt]input.south) -- ([xshift= 6pt]dom.north);
\draw[arrow, -{Latex[length=4pt,width=3.5pt]}]
    ([xshift=-6pt]dom.north)   -- ([xshift=-6pt]input.south);
 
% Boîtes de déclenchement et confirmation
\node[main, minimum width=2.6cm] at (-0.3cm,  2.6cm) (trigger)
    {Déclenchement\\de sauvegarde};
 
\node[main, minimum width=2cm] at (-0.5cm, -1.4cm) (confirm)
    {Sauvegarde\\terminée \& \\ confirmée};
 
%% ═══════════════════════════════════════════════════════
%% COLONNE DROITE — Web Worker
%% ═══════════════════════════════════════════════════════
 
\node[work] at (5.8cm,  2.6cm) (rawdata)
    {Données de sauvegarde\\
     \footnotesize{(objet brut)}};
 
\node[proc] at (5.8cm,  1.2cm) (stringify)
    {\texttt{JSON.stringify(Object)}};
 
\node[proc] at (5.8cm,  0.0cm) (jsonstr)
    {Chaîne JSON résultante};
 
\node[work] at (5.8cm, -1.4cm) (calctime)
    {Temps de calcul\\
     \footnotesize{(non-bloquant pour l'UI)}};
 
% Flèches internes Web Worker
\draw[arrow] (rawdata)   -- (stringify);
\draw[arrow] (stringify) -- (jsonstr);
\draw[arrow] (jsonstr)   -- (calctime);
 
%% ═══════════════════════════════════════════════════════
%% FLÈCHES ASYNCHRONES inter-zones
%% ═══════════════════════════════════════════════════════
 
% Requête : trigger -> rawdata
\draw[asyncarrow] ([yshift=-0.4cm]trigger.east)
    -- node[lblbf, below=3pt, text width=1.7cm, xshift=-0.1cm, fill=clrOutput!40, line width=0.6pt, draw=clrOutput, rounded corners=3pt] {requête asynchrone}
    ([yshift=-0.4cm]rawdata.west);
 
% Réponse : calctime -> confirm
\draw[asyncarrow] ([yshift=-0.4cm]calctime.west)
    -- node[lblbf, below=6pt, text width=1.7cm, xshift=0.2cm, fill=clrOutput!40, line width=0.6pt, draw=clrOutput, rounded corners=3pt] {réponse asynchrone}
    ([yshift=-0.4cm]confirm.east);
 
%% ═══════════════════════════════════════════════════════
%% BOÎTES ENGLOBANTES
%% ═══════════════════════════════════════════════════════
 
\begin{scope}[on background layer]
  \node[draw=clrBorder, rounded corners=6pt,
        fill=clrInput!40, line width=1pt,
        fit=(ui)(input)(dom)(trigger)(confirm),
        inner sep=12pt,
        label={[font=\normalsize\bfseries, text=clrBorder]
               above:Fil principal \normalfont\footnotesize (Main Thread)}
       ] (mainbox) {};
 
  \node[draw=clrBorder, rounded corners=6pt,
        fill=clrToken!40, line width=1pt,
        fit=(rawdata)(stringify)(jsonstr)(calctime),
        inner sep=12pt,
        label={[font=\normalsize\bfseries, text=clrBorder]
               above:Web Worker \normalfont\footnotesize (dédié à la sérialisation)}
       ] (workerbox) {};
\end{scope}
    
    \end{tikzpicture}
    \caption{Architecture de séparation fil principal/Web Worker pour la sauvegarde (Auteur 2026)}
    \label{fig:webworker-architecture}
\end{figure}


L'extrait de code \ref{lst:saveworker} présente les deux composants de cette architecture. Côté fil secondaire, le \texttt{worker} reçoit via \texttt{postMessage} un objet contenant l'état courant et le nom du cookie cible, exécute \texttt{JSON.stringify} dans son propre fil d'exécution, puis retourne la chaîne sérialisée accompagnée du nom du cookie. Ce couplage garantit que le \gls{webworker} ne conserve aucune donnée entre deux appels: il reçoit l'intégralité des informations nécessaires à chaque message et peut être réutilisé pour n'importe quelle opération de sauvegarde sans initialisation supplémentaire. Côté fil principal, le \gls{webworker} est instancié une seule fois à l'initialisation du composant via \texttt{useEffect} et est conservé dans une référence \texttt{useRef} pour éviter des instanciations répétées. La fonction de rappel \texttt{onmessage} reçoit le résultat de manière asynchrone et déclenche l'écriture du cookie sans jamais avoir bloqué le fil de \gls{rendu}.

\begin{listing}[H]
\begin{minted}{ts}
// workers/saveWorkers.ts (fil secondaire)
self.addEventListener("message",
    (e: MessageEvent<SaveWorkerMessage>) => {
        const { state, cookieName } = e.data;
        const serialized = JSON.stringify(state);
        (self as unknown as Worker).postMessage(
            { serialized, cookieName } satisfies SaveWorkerResponse
        );
    }
);


// VisualNovel/index.tsx (fil principal)
const saveWorkerRef = useRef<Worker | null>(null);

useEffect(() => {
    const worker = new Worker(
        new URL('../../../workers/saveWorker.ts', import.meta.url),
        { type: 'module' }
    );
    worker.addEventListener('message',
        (e: MessageEvent<SaveWorkerResponse>) => {
            saveState(e.data.cookieName, e.data.serialized);
        }
    );
    saveWorkerRef.current = worker;
    return () => {
        worker.terminate();
        saveWorkerRef.current = null;
    };
}, []);
\end{minted}
\caption{Implémentation du \texttt{saveWorker}: protocole de communication entre le fil principal et le fil secondaire (Auteur 2026)}
\label{lst:saveworker}
\end{listing}

\clearpage


\subsubsubsection{Check}

Après plusieurs essais du moteur, les fonctionnalités implémentées lors de la première itération ont été vérifiées et se sont révélées non altérées par les modifications apportées.

Afin d'évaluer l'impact des optimisations sur les performances de rendu, cinq sessions de test identiques ont été enregistrées via le \gls{reactprofiler}, sur la version précédente des optimisations (\textit{commit}\footnote{\textbf{Commit}: dans \gls{git}, enregistrement d'un ensemble de modifications apportées au code source à un instant donné, identifié par une empreinte unique (hash) permettant d'y revenir ou de le référencer précisément. \parencite{noauthor_git_nodate}} \texttt{9831b9e}) et sur la version optimisée. Chaque session suivait la même séquence d’interactions sur le même fichier \gls{json} de test, dans le même navigateur.

\clearpage

Les tableaux \ref{tab:profiling_before} et \ref{tab:profiling_after} présentent les quatre métriques relevées: le nombre total de \gls{rendu}, la durée moyenne par \gls{rendu}, la durée maximale observée et la durée cumulée de l'ensemble des \glspl{rendu} de la session.

\begin{table}[H]
    \centering
    \caption{Mesures de performances avant optimisation (cinq sessions)}
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{p{0.8cm} p{2.5cm} p{3cm} p{3cm} p{3cm}}
        \hline
        \textbf{N°} & \textbf{Rendus} & \textbf{Durée moy. (ms)} & \textbf{Durée max. (ms)} & \textbf{Durée totale (ms)} \\
        \hline
        1 & 688 & 0.165 & 2.90 & 113.70 \\
        2 & 689 & 0.166 & 2.90 & 114.50 \\
        3 & 689 & 0.161 & 3.10 & 111.00 \\
        4 & 688 & 0.168 & 3.10 & 115.70 \\
        5 & 688 & 0.163 & 3.00 & 112.10 \\
        \hline
        \textbf{*} & \textbf{688} & \textbf{0.165} & \textbf{3.00} & \textbf{113.70} \\
        \hline
    \end{tabular}
    \par\smallskip{\footnotesize * Médiane des cinq sessions}
    \label{tab:profiling_before}
\end{table}

\begin{table}[H]
    \centering
    \caption{Mesures de performances après optimisation (cinq sessions)}
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{p{0.8cm} p{2.5cm} p{3cm} p{3cm} p{3cm}}
        \hline
        \textbf{N°} & \textbf{Rendus} & \textbf{Durée moy. (ms)} & \textbf{Durée max. (ms)} & \textbf{Durée totale (ms)} \\
        \hline
        1 & 476 & 0.335 & 19.90 & 159.50 \\
        2 & 492 & 0.320 & 18.30 & 157.60 \\
        3 & 492 & 0.327 & 18.40 & 161.10 \\
        4 & 492 & 0.321 & 18.30 & 158.00 \\
        5 & 493 & 0.321 & 18.20 & 158.30 \\
        \hline
        \textbf{*} & \textbf{492} & \textbf{0.321} & \textbf{18.30} & \textbf{158.30} \\
        \hline
    \end{tabular}
    \par\smallskip{\footnotesize * Médiane des cinq sessions}
    \label{tab:profiling_after}
\end{table}

\clearpage

Les figures \ref{fig:profiling_renders} et \ref{fig:profiling_maxdur} illustrent respectivement l'évolution du nombre de \gls{rendu} et de la durée maximale entre les deux versions.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            bar width=0.9cm,
            width=14cm,
            height=8cm,
            ymin=400,
            ymax=750,
            ylabel={Nombre de rendus},
            xtick=data,
            xticklabels={Session 1, Session 2, Session 3, Session 4, Session 5},
            legend style={at={(0.5,-0.18)}, anchor=north, legend columns=2},
            ymajorgrids=true,
            grid style=dashed,
            enlarge x limits=0.15,
        ]
        \addplot[fill=blue!60, draw=blue!80]
            coordinates {(1,688) (2,689) (3,689) (4,688) (5,688)};
        \addplot[fill=teal!60, draw=teal!80]
            coordinates {(1,476) (2,492) (3,492) (4,492) (5,493)};
        \legend{Avant optimisation, Après optimisation}
        \end{axis}
    \end{tikzpicture}
    \caption{Nombre de rendus par session avant et après optimisation (\textit{commit} \texttt{9831b9e}) mesuré via React DevTools Profiler (Auteur 2026)}
    \label{fig:profiling_renders}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            bar width=0.9cm,
            width=14cm,
            height=8cm,
            ymin=0,
            ymax=25,
            ylabel={Durée maximale (ms)},
            xtick=data,
            xticklabels={Session 1, Session 2, Session 3, Session 4, Session 5},
            legend style={at={(0.5,-0.18)}, anchor=north, legend columns=2},
            ymajorgrids=true,
            grid style=dashed,
            enlarge x limits=0.15,
        ]
        \addplot[fill=blue!60, draw=blue!80]
            coordinates {(1,2.9) (2,2.9) (3,3.1) (4,3.1) (5,3.0)};
        \addplot[fill=teal!60, draw=teal!80]
            coordinates {(1,19.9) (2,18.3) (3,18.4) (4,18.3) (5,18.2)};
        \legend{Avant optimisation, Après optimisation}
        \end{axis}
    \end{tikzpicture}
    \caption{Durée maximale de rendu par session avant et après optimisation (\textit{commit} \texttt{9831b9e}) mesurée via React DevTools Profiler (Auteur 2026)}
    \label{fig:profiling_maxdur}
\end{figure}

\clearpage

Les mesures révèlent un résultat nuancé. Le nombre de \glspl{rendu} a diminué de 28.5\% (688 → 492), confirmant l'effet de \texttt{React.memo}: des \glslink{rendu}{re-rendus} inutiles ont été éliminés. En revanche, la durée moyenne par \gls{rendu} a augmenté (0.165ms → 0.321ms), et le pic maximal est passé de 3ms à 18.3ms. Ce comportement est cohérent avec la stratégie de précomputation adoptée par le composant \texttt{Typewriter}: le coût de calcul est concentré sur le \gls{rendu} initial plutôt que distribué sur chaque caractère affiché. Pour des performances globales comparables, la version optimisée impose donc une charge moindre au fil principal du navigateur sur la durée d'une session.


\subsubsubsection{Act}

Les optimisations apportées lors de cette itération ayant atteint leur objectif principal, l'axe de travail suivant était déjà identifié depuis la première itération: le traitement des erreurs et la validation des données. Le moteur étant désormais performant, il convenait de s'assurer qu'il soit également robuste face à des fichiers \gls{json} invalides ou incomplets. La troisième itération a donc été consacrée à la recherche et à la mise en place d'un système de validation du \gls{schema}.


\subsubsection{Troisième itération: la sécurité et la robustesse}

\subsubsubsection{Plan}

L'objectif de cette itération était d'adresser la limite principale identifiée lors des itérations précédentes: l'absence de validation formelle des fichiers \acrshort{json} fournis par l'auteur. Sans mécanisme de vérification, un fichier syntaxiquement  invalide ou non conforme au \gls{schema} attendu pouvait mettre le moteur dans un état indéterminé, sans retour d'information exploitable pour l'auteur. L'intégration de la \gls{librairie} \gls{zod}, présentée au chapitre \ref{sec:zod}, constituait la réponse à cette lacune.

Par ailleurs, la découverte de \gls{zod} n'a pas été le fruit d'une recherche ciblée, mais d'une rencontre fortuite sur GitHub, où la \gls{librairie} apparaissait fréquemment dans des projets \gls{ts} traitant de la validation de \gls{schema}. Avant de l'adopter, deux alternatives ont été évaluées: TypeBox\footnote{\textbf{TypeBox}: \gls{librairie} de validation de \gls{schema} \gls{ts} basé sur \gls{json}, développée par Haydn Paterson \parencite{noauthor_typebox_nodate}}  et ArkType\footnote{\textbf{ArkType}: \gls{librairie} de validation et d'inférence de types pour \gls{ts}, axée sur la performance et une \gls{syntaxe} inspirée de la notion native du langage \parencite{noauthor_arktype_nodate}}. TypeBox propose une approche similaire à \gls{zod}, tandis qu'ArkType repose sur une \gls{syntaxe} sensiblement différente, dont la prise en main s'est révélée moins immédiate. Le choix s'est finalement porté sur \gls{zod} pour la concision et la lisibilité de sa \gls{syntaxe}, un critère important dans l'optique de maintenir un code accessible aux contributeurs potentiels du projet.

\clearpage

Par ailleurs, cette itération a permis d'implémenter plusieurs fonctionnalités \textit{Can} identifiées au tableau \ref{tab:objectifs_moscow}, dont le temps disponible n'avait pas permis l'intégration lors des itérations précédentes: l'affichage des \glsplit{sprite} de personnages avec positionnement dynamique, les transitions entre scènes et dialogues, la gestion des polices d'écriture via des variables \acrshort{css}, et la prise en charge d'un \glsit{placeholder} dynamique pour les champs de saisie.


\subsubsubsection{Do}

L'intégration de \gls{zod} s'est articulée autour de deux \glspl{schema} de validation distincts. Le premier, \texttt{storySchema}, décrit la structure attendue du fichier \acrshort{json} de scénario: types de n\oe{}uds, présence des champs obligatoires, valeurs autorisées pour les propriétés énumérées. Le second, \texttt{saveSchema}, couvre la structure des fichiers de sauvegarde afin de détecter et de rejeter tout fichier corrompu ou modifié manuellement avant qu'il ne soit transmis au moteur. En cas d'écart par rapport au \gls{schema} défini (champ manquant, type incorrect, structure invalide), \gls{zod} retourne un message d'erreur explicite indiquant précisément la localisation et la nature du problème, ce qui constitue un mécanisme de détection d'erreurs déterministe et un retour direct pour l'auteur.

L'extrait de code \ref{lst:storyschema} présente un extrait du \texttt{VNStorySchema}. La validation s'articule en deux niveaux. le premier est déclaratif: chaque champ du fichier \gls{json} est typé explicitement via les primitives \gls{zod} (\texttt{z.string()}, \texttt{z.number().positive()}, \texttt{z.enum([...])}), ce qui permet de détecter à l'exécution tout champ manquant, tout type incorrect ou toute valeur hors des options autorisées. Le second niveau, implémenté via \texttt{superRefine}, effectue une validation sémantique cross-champs impossible à exprimer avec des types seuls: il vérifie que la scène référencée par \texttt{startingScene} existe bien dans le bloc \texttt{story} et que l'ensemble des cibles de navigation (champs \texttt{next} des dialogues et des options) pointe vers des scènes déclarées. En cas d'écart, \texttt{ctx.addIssue} produit un message d'erreur incluant le chemin exact du champ fautif, tel qu'illustré à la figure \ref{fig:zod_erreurs}.

\begin{listing}[H]
\begin{minted}{ts}
const DialogueSchema = z.object({
    next: z.string().optional(),
    options: z.array(OptionSchema).optional(),
});

export const VNStorySchema = z.object({
    settings: SettingsSchema,
    story: z.record(z.string(), SceneTypeSchema),
}).superRefine((data, ctx) => {
    if (!data.story[data.settings.startingScene]) {
        ctx.addIssue({
            code: "custom",
            message: `"${data.settings.startingScene}" does not exist in Story`,
            path: ["settings", "startingScene"],
        });
    }
    for (const [sceneId, scene] of Object.entries(data.story)) {
        scene.dialogues.forEach((dialogue, i) => {
            const refs = [
                ...(dialogue.next ? [{val: dialogue.next, path: ["next"]}] : []),
                ...(dialogue.options ?? []).map(o => ({val: o.next, path: ["options"]})),
            ];
            refs.forEach(({val, path}) => {
                const target = val.split(":")[0];
                if (!data.story[target] && target !== END_STORY_TOKEN) {
                    ctx.addIssue({
                        code: "custom",
                        message: `Scene "${target}" does not exist`,
                        path: ["story", sceneId, "dialogues", i, ...path],
                    });
                }
            });
        });
    }
});
\end{minted}
\caption{Extrait du \texttt{VNStorySchema}: validation déclarative et vérification sémantique des références de navigation (Auteur 2026)}
\label{lst:storyschema}
\end{listing}

\clearpage

Afin de couvrir les cas d'erreur de manière systématique, des fichiers de test dédiés ont été ajoutés au dépôt dans un dossier \texttt{tests/}. Ces fichiers représentent intentionnellement des scénarios invalides: \acrshort{json} à la \gls{syntaxe} cassé (virgule manquante, guillemet non fermé), fichiers avec des champs obligatoires absents, et fichiers avec des valeurs de type incorrect. Ces fichiers sont exclus du \glsit{build} de production via un \glsit{plugin} \gls{vite} dédié.


\subsubsubsection{Check}

Les \glslink{negativetesting}{tests négatifs} réalisés à l'issue de cette itération ont couvert l'ensemble des cas décrits ci-dessus. Dans chaque cas, \gls{zod} a retourné un message d'erreur explicite et le moteur est resté dans un état stable, confirmant le comportement attendu, comme illustré à la figure \ref{fig:zod_erreurs}.

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.47\textwidth}
        \includegraphics[height=4.85cm]{img/zod_invalid_syntax_1.png}
        \caption{Erreur de syntaxe JSON}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.47\textwidth}
        \includegraphics[height=4.85cm]{img/zod_missing_field_1.png}
        \caption{Champs obligatoires omis}
    \end{subfigure}
    \caption{Messages d'erreur retournés par Zod lors de la validation du fichier JSON (Auteur 2026)}
    \label{fig:zod_erreurs}
\end{figure}

La validation des fichiers de sauvegarde a également été testée en fournissant des fichiers modifiés manuellement, qui ont été correctement rejetés avec un message d'erreur remontant jusqu'à l'écran titre.

\clearpage

La figure \ref{fig:NamelessStoryFinal} présente l'état du moteur à l'issue de ces trois itérations. La remarque formulée en note de bas de page \ref{fn:zoom175} au sujet du zoom du navigateur vaut également pour ces captures. Le fichier \gls{json} utilisé pour cette capture diffère de celui de la première itération: plutôt qu'une courte histoire, il s'agit d'une démonstration (\textit{"The Feature Tour"}) parcourant l'ensemble des fonctionnalités implémentées dans le moteur.

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.47\textwidth}
        \includegraphics[width=\textwidth]{img/NamelessStory_final_home.png}
        \caption{Écran titre}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.47\textwidth}
        \includegraphics[width=\textwidth]{img/NamelessStory_final_text.png}
        \caption{Affichage d'un dialogue}
    \end{subfigure}
    \caption{\nsits à l'issue de la troisième itération (Auteur 2026)}
    \label{fig:NamelessStoryFinal}
\end{figure}


\subsubsubsection{Act}

À l'issue de cette itération, l'ensemble des objectifs \textit{Must} et \textit{Should} définis au tableau \ref{tab:objectifs_moscow} avaient été atteints. Plusieurs fonctionnalités \textit{Can} ont également pu être intégrées. Les fonctionnalités restantes hors périmètre (notamment un éditeur de scénario intégré et une gestion avancée des embranchements narratifs complexes) sont identifiées comme axes de développement futurs dans le chapitre \ref{chap:resultats}.